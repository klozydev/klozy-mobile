/// Fired on the app [EventBus] after the user adds or removes a product from
/// their wishlist — open screens that display wishlist state reload quietly.
class WishlistChangedEvent {
  const WishlistChangedEvent();
}
