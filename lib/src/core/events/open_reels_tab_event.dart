/// Fired on the app [EventBus] when a flow wants the Home shell to jump to the
/// Reels tab — e.g. after a reel is posted from the composer.
class OpenReelsTabEvent {
  const OpenReelsTabEvent();
}
